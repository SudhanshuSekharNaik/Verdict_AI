# =====================================================================
# VerdictAI — Production Backend Requirements
# =====================================================================
# This file intentionally mirrors backend/requirements.txt.
# Render uses: buildCommand: pip install -r backend/requirements.txt
# (configured in render.yaml)
#
# Heavy ML/AI research dependencies (torch, transformers, langchain,
# langgraph, spacy, sentence-transformers, datasets, etc.) are NOT
# needed at runtime — VerdictAI uses Groq API for all inference.
# Those are available in requirements-dev.txt for local experimentation.
# =====================================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# LLM inference (Groq API — no local model weights needed)
groq==0.11.0

# HTTP client
httpx==0.27.0

# Data validation
pydantic==2.9.2

# Environment
python-dotenv==1.0.1

# HTTP utilities
requests==2.32.3

# Async static file serving (required by FastAPI StaticFiles)
aiofiles==24.1.0
